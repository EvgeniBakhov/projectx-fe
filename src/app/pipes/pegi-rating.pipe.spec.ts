import { PegiRatingPipe } from './pegi-rating.pipe';

describe('PegiRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new PegiRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
